# nvmman
Rust TUI NVM Manager - Manage Node.js NVM versions and NPM packages - Interactive nvm maintainer: installs and verifies native platform-specific latest Node LTS, migrates global npm packages, keeps a consolidated JSON registry in ~/.nvm  from every nvm version installed on the machine, restores it to the chosen default Node, syncs/updates packages.
